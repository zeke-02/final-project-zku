import {ethers, Contract, providers} from "ethers";

const BOARD_ADDRESS = "REPLACE WITH CONTRACT ADDRESS";

async function getGroupInfo(name: string) {
    // filter for past events: MessageAdded, GroupCreated, with corresponding groupnam, reading from the bk so no gas.
    
}
